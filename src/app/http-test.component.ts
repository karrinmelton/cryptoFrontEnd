import { Component } from '@angular/core';
import { Http, Response } from '@angular/http';
import 'rxjs/add/operator/map' ;

@Component({
    selector: 'http-test',
    templateUrl: 'app.component.html',
    // template: <button>Test GET RE
})

export class AppComponent { 
  
}